-- -------------------------------------------------------------------------
--	文件名		：	Util.lua
--	创建者		：	Sundyhy
--	创建时间	：	2013/12/4 12:15:07
--	功能描述	：	辅助函数定义
--
-- -----------------------------------------------------------------------*/

clickFunc = function(obj, func, ...)
    local args = {...};
    return function(tag)
        func(obj, tag, unpack(args));
    end
end

